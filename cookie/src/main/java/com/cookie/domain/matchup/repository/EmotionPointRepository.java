package com.cookie.domain.matchup.repository;

import com.cookie.domain.matchup.entity.EmotionPoint;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EmotionPointRepository extends JpaRepository<EmotionPoint, Long> {
}
